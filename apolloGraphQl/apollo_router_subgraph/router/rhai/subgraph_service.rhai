// Module file

fn process_request(request) {
  print(`Subgraph service: Ready to send sub-operation to subgraph`);
}

fn process_response(response) {
  print(`Subgraph service: Received sub-operation response`);
}